# ============================================
# CONFIGURACIÓN DE DJANGO
# ============================================

# SECRET_KEY: Clave secreta para Django (¡CAMBIAR EN PRODUCCIÓN!)
# Genera una nueva con: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=django-insecure-ejemplo-cambiar-en-produccion

# DEBUG: Modo de desarrollo (True) o producción (False)
# ⚠️  IMPORTANTE: Establecer en False en producción
DEBUG=True

# ALLOWED_HOSTS: Hosts/dominios permitidos (separados por coma, sin espacios)
# Ejemplos:
#   - Desarrollo: localhost,127.0.0.1
#   - Producción: tudominio.com,www.tudominio.com,api.tudominio.com
ALLOWED_HOSTS=localhost,127.0.0.1

# ============================================
# CONFIGURACIÓN DE BASE DE DATOS
# ============================================

# DATABASE_ENGINE: Motor de base de datos
# Opciones:
#   - django.db.backends.sqlite3 (desarrollo - por defecto)
#   - django.db.backends.postgresql (producción recomendado)
#   - django.db.backends.mysql
#   - django.db.backends.oracle
# Si no se especifica, usa SQLite por defecto
# DATABASE_ENGINE=django.db.backends.sqlite3

# ----------------------
# SQLite (Desarrollo)
# ----------------------
# DATABASE_NAME: Ruta al archivo de base de datos SQLite
# Dejar vacío o comentar para usar la ruta por defecto: /backend/db.sqlite3
# Ejemplo de ruta personalizada:
# DATABASE_NAME=/home/usuario/techtrace/backend/database.db

# ----------------------
# PostgreSQL (Producción)
# ----------------------
# Descomentar y configurar estas variables para usar PostgreSQL:

# DATABASE_ENGINE=django.db.backends.postgresql
# DATABASE_NAME=techtrace_db
# DATABASE_USER=techtrace_user
# DATABASE_PASSWORD=tu_password_seguro_aqui
# DATABASE_HOST=localhost
# DATABASE_PORT=5432

# Ejemplos de configuración PostgreSQL:
#
# Servidor local:
#   DATABASE_HOST=localhost
#   DATABASE_PORT=5432
#
# Servidor remoto:
#   DATABASE_HOST=db.tudominio.com
#   DATABASE_PORT=5432
#
# Docker Compose:
#   DATABASE_HOST=postgres (nombre del servicio)
#   DATABASE_PORT=5432
#
# Heroku/Railway/Render:
#   Usa DATABASE_URL (ver abajo)

# ----------------------
# MySQL/MariaDB (Alternativa)
# ----------------------
# Descomentar y configurar estas variables para usar MySQL:

# DATABASE_ENGINE=django.db.backends.mysql
# DATABASE_NAME=techtrace_db
# DATABASE_USER=techtrace_user
# DATABASE_PASSWORD=tu_password_seguro_aqui
# DATABASE_HOST=localhost
# DATABASE_PORT=3306

# ----------------------
# DATABASE_URL (Opcional)
# ----------------------
# Algunos servicios de hosting (Heroku, Railway, Render) usan DATABASE_URL
# Formato: postgresql://usuario:password@host:puerto/nombre_db
# Si se usa DATABASE_URL, se ignoran las otras variables de DB
# DATABASE_URL=postgresql://techtrace_user:password@localhost:5432/techtrace_db

# ============================================
# CONFIGURACIÓN DE CORS
# ============================================

# CORS_ALLOWED_ORIGINS: Orígenes permitidos para peticiones CORS (separados por coma, sin espacios)
# Ejemplos:
#   - Desarrollo: http://localhost:3000,http://127.0.0.1:3000
#   - Producción: https://tudominio.com,https://www.tudominio.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# CORS_ALLOW_CREDENTIALS: Permitir envío de cookies y credenciales (True/False)
CORS_ALLOW_CREDENTIALS=True

# ============================================
# CONFIGURACIÓN DE JWT
# ============================================

# JWT_ACCESS_TOKEN_LIFETIME: Duración del token de acceso (en minutos)
JWT_ACCESS_TOKEN_LIFETIME=60

# JWT_REFRESH_TOKEN_LIFETIME: Duración del token de refresh (en días)
JWT_REFRESH_TOKEN_LIFETIME=7

# ============================================
# INTERNACIONALIZACIÓN
# ============================================

# LANGUAGE_CODE: Código de idioma del sistema
# Opciones: es-cl, es-es, es-mx, en-us, etc.
LANGUAGE_CODE=es-cl

# TIME_ZONE: Zona horaria del sistema
# Ejemplos: America/Santiago, America/Lima, America/Mexico_City, UTC
TIME_ZONE=America/Santiago

# USE_TZ: Usar zonas horarias (True recomendado)
USE_TZ=True

# ============================================
# CONFIGURACIÓN DE EMAIL (Opcional)
# ============================================

# Descomentar y configurar para habilitar envío de correos

# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=tu-email@gmail.com
# EMAIL_HOST_PASSWORD=tu-password-de-aplicacion
# DEFAULT_FROM_EMAIL=TechTrace <noreply@tudominio.com>

# ============================================
# CONFIGURACIÓN DE ARCHIVOS ESTÁTICOS Y MEDIA
# ============================================

# STATIC_URL: URL base para archivos estáticos
STATIC_URL=/static/

# STATIC_ROOT: Directorio donde se recopilan archivos estáticos (producción)
# STATIC_ROOT=/var/www/techtrace/static/

# MEDIA_URL: URL base para archivos subidos por usuarios
MEDIA_URL=/media/

# MEDIA_ROOT: Directorio donde se almacenan archivos subidos
# MEDIA_ROOT=/var/www/techtrace/media/

# ============================================
# CONFIGURACIÓN DE SEGURIDAD (Producción)
# ============================================

# Descomentar estas opciones en producción:

# SECURE_SSL_REDIRECT=True
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True
# SECURE_BROWSER_XSS_FILTER=True
# SECURE_CONTENT_TYPE_NOSNIFF=True
# X_FRAME_OPTIONS=DENY
# SECURE_HSTS_SECONDS=31536000
# SECURE_HSTS_INCLUDE_SUBDOMAINS=True
# SECURE_HSTS_PRELOAD=True

# ============================================
# LOGGING Y MONITOREO
# ============================================

# LOG_LEVEL: Nivel de logging (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# SENTRY_DSN: URL de Sentry para monitoreo de errores (opcional)
# SENTRY_DSN=https://tu-dsn@sentry.io/proyecto

# ============================================
# VARIABLES ESPECÍFICAS DE LA APLICACIÓN
# ============================================

# FRONTEND_URL: URL del frontend (para enlaces en emails, PDFs, etc.)
FRONTEND_URL=http://localhost:3000

# MAX_UPLOAD_SIZE: Tamaño máximo de archivos subidos (en MB)
MAX_UPLOAD_SIZE=10

# DEVICE_DEPRECIATION_YEARS: Años para depreciación de dispositivos
DEVICE_DEPRECIATION_YEARS=3